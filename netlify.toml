# Redirect support for storybook/core demo
[[redirects]]
  from = "/*"
  to = "/"
  status = 200

# Support CORS on JSON files
[[headers]]
  for = "/*.json"
    [headers.values]
    Access-Control-Allow-Origin = "*"
