import { AfterViewInit, ChangeDetectorRef } from "@angular/core";
import { Comparator } from "./interfaces/comparator";
import { Sort } from "./providers/sort";
export declare class DatagridColumn implements AfterViewInit {
    private _sort;
    private _cdr;
    constructor(_sort: Sort, _cdr: ChangeDetectorRef);
    ngAfterViewInit(): void;
    /**
     * Comparator to use when sorting the column
     */
    sortBy: Comparator<any>;
    /**
     * Indicates if the column is sortable
     */
    readonly sortable: boolean;
    /**
     * Sorts the datagrid based on this column
     */
    sort(): void;
    /**
     * Indicates if the column is currently sorted
     */
    readonly sorted: boolean;
    /**
     * Indicates if the column is currently sorted in ascending order
     */
    readonly asc: boolean;
    /**
     * Indicates if the column is currently sorted in descending order
     */
    readonly desc: boolean;
    /**
     * A custom filter for this column that can be provided in the projected content
     */
    private customFilter;
    projectedFilter: any;
    /**
     * Default property filter when using the object property shortcut
     */
    private defaultFieldFilter;
    private _field;
    field: string;
}
