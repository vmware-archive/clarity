import { ChangeDetectorRef, DoCheck, IterableDiffers, TemplateRef, TrackByFn } from "@angular/core";
import { Items } from "./providers/items";
export declare class DatagridItems implements DoCheck {
    template: TemplateRef<any>;
    private _differs;
    private _changeDetector;
    private _items;
    private _rawItems;
    private _differ;
    constructor(template: TemplateRef<any>, _differs: IterableDiffers, _changeDetector: ChangeDetectorRef, _items: Items);
    items: any[];
    /**
     * Tracking function to identify objects. Angular's default is reference equality.
     */
    trackBy: TrackByFn;
    ngDoCheck(): void;
}
