import '@cds/core/alert/register.js';
import { Meta, Props, Story, Preview, API } from '@storybook/addon-docs/blocks';
import { html, LitElement } from 'lit-element';

<Meta title="Components/Alert" />

# Alert

Alerts communicate a message with a severity attached to it. They grab the user’s
attention to provide critical information needed in context. If the alerts are dynamic,
their container will need to have an `aria-live` attribute set on them so that
screen-readers will announce content changes within the container.

## Installation

To use the alert component import the component in your JavaScript.

```typescript
import '@cds/core/alert/register.js';
```

```html
<cds-alert-group type="default" status="info">
  <cds-alert>
    This is an alert. It will display a decorative "info" icon by default. You should set the aria-live attribute on the
    alert to a level that aligns with the severity of the alert's status.
  </cds-alert>
  <cds-alert>
    This is a second alert. It will display a decorative "info" icon by default.
  </cds-alert>
</cds-alert-group>
```

## Standard Alert

<Preview>
  <Story id="stories-alert-group--alert-group" />
</Preview>

## Compact

<Preview>
  <Story id="stories-alert-group--compact-standard-alert-group" />
</Preview>

## Dark Theme

<Preview>
  <Story id="stories-alert-group--dark-theme-standard-alert-group" />
</Preview>
