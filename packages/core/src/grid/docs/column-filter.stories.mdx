import { Meta, ArgsTable, Story, Preview } from '@web/storybook-prebuilt/addon-docs/blocks.js';

<Meta title="Internal Preview Grid/Column Filter" />

# Grid Column Filter

<cds-alert-group status="warning" cds-layout="m-b:lg">
  <cds-alert>
    This component or utility is offered as a preview. This means we are currently working on it and seeking feedback.
    Please be aware that this component or utility may have breaking changes before we finish working on it.
  </cds-alert>
</cds-alert-group>

To use column filtering leverage the `cds-search` or `cds-datalist` components. With `cds-button-action`
toggle a filter dopdown to filter a particular column. Using the search input events
filter against your list based on the applied column.

<Story id="stories-grid--column-filter" />

```html
<cds-grid>
  <cds-grid-column>
    Host <cds-button-action id="id-filter-demo" shape="filter" aria-label="search available hosts"></cds-button-action>
  </cds-grid-column>
  <cds-grid-column>Status</cds-grid-column>
  <cds-grid-column>CPU</cds-grid-column>
  <cds-grid-column>Memory</cds-grid-column>
  <cds-grid-row>
    <cds-grid-cell>vm-host-001</cds-grid-cell>
    <cds-grid-cell>online</cds-grid-cell>
    <cds-grid-cell>5%</cds-grid-cell>
    <cds-grid-cell>10%</cds-grid-cell>
  </cds-grid-row>
</cds-grid>
<cds-dropdown hidden anchor="#id-filter-demo (element ref)">
  <cds-search>
    <input type="search" aria-label="search hosts" placeholder="Search" />
  </cds-search>
</cds-dropdown>
```

## API

### cds-grid

<ArgsTable of={'cds-grid'} />

### cds-grid-column

<ArgsTable of={'cds-grid-column'} />

### cds-grid-row

<ArgsTable of={'cds-grid-row'} />

### cds-grid-cell

<ArgsTable of={'cds-grid-cell'} />

### cds-grid-footer

<ArgsTable of={'cds-grid-footer'} />
