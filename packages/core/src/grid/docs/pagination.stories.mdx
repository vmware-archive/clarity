import { Meta, ArgsTable, Story, Preview } from '@web/storybook-prebuilt/addon-docs/blocks.js';

<Meta title="Internal Preview Grid/Pagination" />

# Grid Pagination

<cds-alert-group status="warning" cds-layout="m-b:lg">
  <cds-alert>
    This component or utility is offered as a preview. This means we are currently working on it and seeking feedback.
    Please be aware that this component or utility may have breaking changes before we finish working on it.
  </cds-alert>
</cds-alert-group>

The `cds-grid-pagination` component provides a standard pagination UI to the datagrid footer.
It provides stateless properties on setting the active pagination state and events like `pageChange` and `pageSizeChange`.
If you want a more customizable pagination UI you can swap `cds-grid-pagination` for the stand alone
[cds-pagination](./?path=/story/components-pagination--page) component.

```html
<cds-grid>
  <cds-grid-column>Host</cds-grid-column>
  <cds-grid-column>Status</cds-grid-column>
  <cds-grid-column>CPU</cds-grid-column>
  <cds-grid-column>Memory</cds-grid-column>
  <cds-grid-row>
    <cds-grid-cell>vm-host-001</cds-grid-cell>
    <cds-grid-cell>online</cds-grid-cell>
    <cds-grid-cell>25%</p></cds-grid-cell>
    <cds-grid-cell>50%</p></cds-grid-cell>
  </cds-grid-row>
  <cds-grid-footer>
    <cds-grid-pagination page="1" page-size="20" page-count="100"></cds-grid-pagination>
  </cds-grid-footer>
</cds-grid>
```

<Story id="stories-grid--pagination" />

## Advanced

If you would like more control of the pagination logic, you can use the generic `cds-pagination` component
in place of `cds-grid-pagination`.

```html
<cds-grid-footer>
  <cds-pagination aria-label="pagination">
    <cds-select>
      <select aria-label="per page">
        <option value="5">5</option>
        <option value="10" selected>10</option>
        <option value="15">15</option>
      </select>
    </cds-select>
    <cds-pagination-button action="first" aria-label="go to first"></cds-pagination-button>
    <cds-pagination-button action="prev" aria-label="go to previous"></cds-pagination-button>
    <cds-input cds-pagination-number>
      <input type="number" value="1" size="1" aria-label="current page" min="1" max="5" />
      <cds-control-message>/ 5</cds-control-message>
    </cds-input>
    <cds-pagination-button action="next" aria-label="go to next"></cds-pagination-button>
    <cds-pagination-button action="last" aria-label="go to last"></cds-pagination-button>
  </cds-pagination>
</cds-grid-footer>
```

## API

### cds-grid-pagination

<ArgsTable of={'cds-grid-pagination'} />

### cds-grid

<ArgsTable of={'cds-grid'} />

### cds-grid-column

<ArgsTable of={'cds-grid-column'} />

### cds-grid-row

<ArgsTable of={'cds-grid-row'} />

### cds-grid-cell

<ArgsTable of={'cds-grid-cell'} />

### cds-grid-footer

<ArgsTable of={'cds-grid-footer'} />
