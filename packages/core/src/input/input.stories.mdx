import { Meta, Props, Story, Preview, API } from '@storybook/addon-docs/blocks';

<Meta title="Forms/Input" />

# Input

Text inputs are for relatively short content, such as a single line of text. For
longer input content, such as paragraphs, consider using a textarea.

## Installation

To use the input component import the component in your JavaScript.

```typescript
import '@cds/core/input/register.js';
```

```html
<cds-input>
  <label>text input</label>
  <input type="text" />
  <cds-control-message>message text</cds-control-message>
</cds-input>
```

<Preview>
  <Story id="stories-input--input" />
</Preview>

## Vertical

<Preview>
  <Story id="stories-input--vertical" />
</Preview>

## Horizontal

<Preview>
  <Story id="stories-input--horizontal" />
</Preview>

## Compact

<Preview>
  <Story id="stories-input--compact" />
</Preview>

## Control Width

By default `cds-input` will fill its parent container. Inputs can use the compact
width `control-width="shrink"` which will set the width to the default browser width.
You can apply custom widths by setting a max width on the component.

<Preview>
  <Story id="stories-input--input-width" />
</Preview>

## Supported Text Types

The `cds-input` component supports a variety of native text based input types.

<Preview>
  <Story id="stories-input--supported-text-types" />
</Preview>

## Dark Theme

<Preview>
  <Story id="stories-input--dark-theme" />
</Preview>

## Input API

<Props of={'cds-input'} />

## Control API

<Props of={'cds-control'} />

## Control Message API

<Props of={'cds-control-message'} />
