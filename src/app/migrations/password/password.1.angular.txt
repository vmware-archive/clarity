import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <form clrForm>
      <clr-password-container>
        <label>Password</label>
        <input clrPassword placeholder="Type a password" name="password" />
      </clr-password-container>
    </form>
  `,
})
export class AppComponent {
}
