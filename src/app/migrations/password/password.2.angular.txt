import { Component } from '@angular/core';
import { FormGroup, FormControl } from '@angular/forms';

@Component({
  selector: 'app-root',
  template: `
    <form [formGroup]="model" clrForm>
      <clr-password-container>
        <label>Password with hint</label>
        <input clrPassword placeholder="Type a password" formControlName="input" required />
        <clr-control-helper>message text</clr-control-helper>
        <clr-control-error>error text</clr-control-error>
      </clr-password-container>
    </form>
  `,
})
export class AppComponent {
  model = new FormGroup({
    input: new FormControl(''),
  });
}
